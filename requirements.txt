# Core dependencies
requests>=2.31.0
pyyaml>=6.0.1
pandas>=2.1.0
beautifulsoup4>=4.12.0
erddapy>=2.2.0

# HTTP client (used by erddapy)
httpx>=0.25.0

# Delivery (optional - only needed if sending notifications)
twilio>=8.10.0
sendgrid>=6.10.0
